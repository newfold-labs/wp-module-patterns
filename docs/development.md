# Development

PHP: `composer run lint`, `composer run clean` (fix). Tests: `composer run test`. When changing dependencies, update [dependencies.md](dependencies.md). When cutting a release, update **docs/changelog.md**.
